<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Core Network Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Location Permissions for SOS Alerts -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    
    <!-- Bluetooth & Nearby Connections Permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />
    
    <!-- Audio & Hardware Permissions -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- Firebase & Notification Permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".SosApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SOSNetwork">

        <!-- Main Launch Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Registration Activity -->
        <activity
            android:name=".ui.RegistrationActivity"
            android:exported="false"
            android:label="@string/register_title"
            android:screenOrientation="portrait" />

        <!-- SOS Alert Activity (Always on Top on Lock Screen) -->
        <activity
            android:name=".ui.SosAlertActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.SOSNetwork.Alert">
            <!-- Firebase Cloud Messaging Deep Links -->
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
            <!-- SOS Alert Deep Link for quick opening -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="sosalert" android:host="sos" />
            </intent-filter>
        </activity>

        <!-- Admin Dashboard -->
        <activity
            android:name=".ui.AdminDashboardActivity"
            android:exported="false"
            android:label="@string/admin_title"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.SOSNetwork.WithActionBar" />

        <!-- Alert History -->
        <activity
            android:name=".ui.AlertHistoryActivity"
            android:exported="false"
            android:label="@string/alert_history_title"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.SOSNetwork.WithActionBar" />

        <!-- Firebase Cloud Messaging Service (for background notifications) -->
        <service
            android:name=".firebase.FcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- SOS Mesh Service (P2P Nearby Connections) -->
        <service
            android:name=".mesh.SosMeshService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|location"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.google.android.gms.nearby.CONNECTIONS_SERVICE" />
            </intent-filter>
        </service>

        <!-- Pushover Broadcast Service (Alternative Notification) -->
        <service
            android:name=".push.PushoverBroadcastService"
            android:exported="false"
            android:foregroundServiceType="notNotification" />

    </application>
</manifest>
